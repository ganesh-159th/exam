package com.company;
import java.util.Scanner;


public class practice_note {
    public static void main(String[] args) {
        /*
        float []a={1.23f,6.4f,6.45f,7.89f,8.9f};
        float sum =0;
        for(float elemnt:a)
        {
            sum =sum+elemnt;

        }
        System.out.println("\n the sum of the elemte in the flaot from is :"+sum );


         */
        /*
        int  mat[][]={{1,2,3},
                        {4,5,6} };
        int  mat1[][]={{2,3,4},
                        {5,6,7}};
        int result [][]={{0,0,0,},
                          {0,0,0}};
     for(int i=0;i<mat.length;i++)
     {
         for(int j=0;j<mat[i].length;j++){
             result[i][j]=mat[i][j] + mat1[i][j];
             System.out.println(result[i][j]+" ");

         }
         System.out.println("\t\n ");

     }

         */
        /*
        float []a={1.23f,6.4f,6.45f,7.89f,8.9f};
        Scanner sc= new Scanner(System.in );

        System.out.println("\n enter the searching element :");
        float b=sc.nextInt();
       for(float element :a)
        {
            if(b==a.length)
            {
                System.out.println("\n the given element has been found :");
            }
            else
            {
                System.out.println(("\n the element has not found :"));
            }

        }
        System.out.println("\n the end of  the program :");


         */
    }
}
